# MCP config for this project

`.cursor/mcp.json` in this directory registers the **GitHub MCP server** (releases, issues, PRs). Prerequisites and token setup: see [docs/AGENT.md](../docs/AGENT.md#mcp).
